---
description: "Verify tests"
---

Invoke the `test-verifier` agent to validate test quality and generate a verification report.

**Arguments:** $ARGUMENTS (required) - Path to `test-plan.md` file

Example: `/verify-tests ./test/context/test-context-products-01/test-plan.md`
